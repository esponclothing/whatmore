const fs = require('fs'); let lines = fs.readFileSync('src/lib/whatsappAI.ts', 'utf8').split('\n'); lines[46] = '  const userMsgs = messages.filter(m => m.role !== \'system\').map(m => (m.role === \'user\' ? \'Customer: \' : \'Agent: \') + m.content).join(String.fromCharCode(10));'; lines[47] = ''; fs.writeFileSync('src/lib/whatsappAI.ts', lines.join('\n'), 'utf8'); console.log('Successfully fixed whatsappAI.ts');
